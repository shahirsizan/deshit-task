export const theme = {
	extend: {
		animation: {
			// from `index.css`
			float1: "float1 6s ease-in-out infinite",
			float2: "float2 8s ease-in-out infinite",
			float3: "float3 5s ease-in-out infinite",
		},
		// keyframes: {
		// 	not needed because already prescribed in `index.css`
		// },
	},
};
export const plugins = [];
